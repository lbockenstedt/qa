[pytest]
testpaths = tests

# TestScenario and TestEngine in qa_engine.py are PRODUCT classes, not pytest
# suites; without this pytest tries to collect them and warns that it cannot
# (they take constructor args). The suite is function-based, so disabling
# class-prefix collection costs nothing.
python_classes =
